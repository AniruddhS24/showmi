# Skills Directory

Place `.md` skill files here. They are automatically loaded and injected into the agent's system prompt.

## Format

Each skill file should contain:
- A title (H1)
- When to use it
- Step-by-step instructions
- Any domain-specific knowledge (selectors, URLs, etc.)
